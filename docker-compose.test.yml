services:
  test_db:
    image: postgres:15-alpine
    container_name: silver_test_db
    env_file:
      - .env.test
    ports:
      - "5436:5432"
    volumes:
      - test_postgres_data:/var/lib/postgresql/data

volumes:
  test_postgres_data: